<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تحليل ومتابعة الشكاوى - مجمع مكة الطبي</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800&family=Tajawal:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        :root {
            --crimson: #9E1B32;
            --blue: #0056A3;
            --blue-dark: #003E75;
            --white: #FFFFFF;
            --off-white: #F8F9FA;
            --gray-light: #E9ECEF;
            --gray: #6C757D;
            --gray-dark: #343A40;
            --success: #28a745;
            --warning: #ffc107;
            --danger: #dc3545;
            --orange: #fd7e14;
            --purple: #6f42c1;
            --shadow-sm: 0 2px 8px rgba(0,0,0,0.08);
            --shadow-md: 0 4px 16px rgba(0,0,0,0.12);
            --shadow-lg: 0 8px 32px rgba(0,0,0,0.18);
            --radius-md: 10px;
            --radius-lg: 14px;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Tajawal', sans-serif;
            background: #f0f2f5;
            min-height: 100vh;
        }
        
        .login-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--blue-dark) 0%, var(--crimson) 100%);
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .login-overlay.hidden { display: none; }
        .login-card {
            background: white;
            border-radius: var(--radius-lg);
            padding: 40px;
            max-width: 420px;
            width: 100%;
            box-shadow: var(--shadow-lg);
            text-align: center;
        }
        .login-logo {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--blue), var(--crimson));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            color: white;
            font-size: 2rem;
        }
        .login-title {
            font-family: 'Cairo', sans-serif;
            font-size: 1.4rem;
            color: var(--blue-dark);
            margin-bottom: 8px;
        }
        .login-subtitle {
            color: var(--gray);
            margin-bottom: 30px;
        }
        .login-select {
            width: 100%;
            padding: 14px 16px;
            border: 2px solid var(--gray-light);
            border-radius: var(--radius-md);
            font-family: 'Tajawal', sans-serif;
            font-size: 1rem;
            margin-bottom: 20px;
            cursor: pointer;
            transition: border-color 0.3s;
        }
        .login-select:focus {
            outline: none;
            border-color: var(--blue);
        }
        .login-btn {
            width: 100%;
            padding: 14px;
            background: linear-gradient(135deg, var(--blue), var(--crimson));
            color: white;
            border: none;
            border-radius: var(--radius-md);
            font-family: 'Tajawal', sans-serif;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .login-btn:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }
        .login-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }
        .login-error {
            color: var(--danger);
            margin-top: 15px;
            display: none;
        }
        
        .header {
            background: linear-gradient(135deg, var(--blue-dark), var(--crimson));
            color: white;
            padding: 15px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 15px;
        }
        .header-title {
            display: flex;
            align-items: center;
            gap: 12px;
        }
        .header-title h1 {
            font-family: 'Cairo', sans-serif;
            font-size: 1.4rem;
        }
        .header-user {
            display: flex;
            align-items: center;
            gap: 10px;
            background: rgba(255,255,255,0.15);
            padding: 8px 15px;
            border-radius: 25px;
        }
        .header-user i { font-size: 1.2rem; }
        .header-actions {
            display: flex;
            gap: 10px;
        }
        .btn {
            padding: 10px 18px;
            border: none;
            border-radius: var(--radius-md);
            font-family: 'Tajawal', sans-serif;
            font-weight: 600;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s;
        }
        .btn-light { background: rgba(255,255,255,0.2); color: white; }
        .btn-light:hover { background: rgba(255,255,255,0.3); }
        .btn-primary { background: var(--blue); color: white; }
        .btn-primary:hover { background: var(--blue-dark); }
        .btn-success { background: var(--success); color: white; }
        .btn-warning { background: var(--warning); color: #333; }
        .btn-danger { background: var(--danger); color: white; }
        .btn-secondary { background: var(--gray-light); color: var(--gray-dark); }
        .btn-sm { padding: 8px 14px; font-size: 0.85rem; }
        
        .container { max-width: 1400px; margin: 0 auto; padding: 20px; }
        
        .tabs {
            display: flex;
            gap: 5px;
            background: var(--white);
            padding: 6px;
            border-radius: var(--radius-lg);
            margin-bottom: 20px;
            box-shadow: var(--shadow-sm);
            overflow-x: auto;
        }
        .tab-btn {
            flex: 1;
            padding: 12px;
            border: none;
            background: transparent;
            border-radius: var(--radius-md);
            cursor: pointer;
            font-family: 'Tajawal', sans-serif;
            font-weight: 600;
            color: var(--gray);
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            white-space: nowrap;
        }
        .tab-btn:hover { background: var(--gray-light); }
        .tab-btn.active { background: var(--blue); color: white; }
        .tab-btn .count {
            background: rgba(0,0,0,0.1);
            padding: 2px 8px;
            border-radius: 10px;
            font-size: 0.8rem;
        }
        .tab-btn.active .count { background: rgba(255,255,255,0.3); }
        
        .tab-content { display: none; }
        .tab-content.active { display: block; }
        
        .stats-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
            gap: 15px;
            margin-bottom: 20px;
        }
        .stat-card {
            background: var(--white);
            border-radius: var(--radius-lg);
            padding: 20px;
            text-align: center;
            box-shadow: var(--shadow-sm);
            border-top: 4px solid var(--blue);
        }
        .stat-card.danger { border-top-color: var(--danger); }
        .stat-card.warning { border-top-color: var(--warning); }
        .stat-card.success { border-top-color: var(--success); }
        .stat-card.orange { border-top-color: var(--orange); }
        .stat-number {
            font-family: 'Cairo', sans-serif;
            font-size: 2rem;
            font-weight: 800;
            color: var(--blue-dark);
        }
        .stat-label { color: var(--gray); font-size: 0.9rem; }
        
        .card {
            background: var(--white);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            margin-bottom: 20px;
            overflow: hidden;
        }
        .card-header {
            padding: 16px 20px;
            border-bottom: 1px solid var(--gray-light);
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 10px;
        }
        .card-title {
            font-family: 'Cairo', sans-serif;
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--blue-dark);
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .card-title i { color: var(--crimson); }
        .card-body { padding: 20px; }
        
        .table-container { overflow-x: auto; }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 12px;
            text-align: right;
            border-bottom: 1px solid var(--gray-light);
        }
        th {
            background: var(--off-white);
            font-weight: 600;
            color: var(--blue-dark);
            white-space: nowrap;
        }
        tr:hover { background: var(--off-white); }
        
        .badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }
        .badge-success { background: rgba(40,167,69,0.15); color: #28a745; }
        .badge-warning { background: rgba(255,193,7,0.2); color: #856404; }
        .badge-danger { background: rgba(220,53,69,0.15); color: #dc3545; }
        .badge-info { background: rgba(0,86,163,0.15); color: #0056A3; }
        .badge-secondary { background: var(--gray-light); color: var(--gray); }
        
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            z-index: 1000;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .modal.active { display: flex; }
        .modal-content {
            background: var(--white);
            border-radius: var(--radius-lg);
            max-width: 800px;
            width: 100%;
            max-height: 90vh;
            overflow-y: auto;
        }
        .modal-header {
            padding: 18px 24px;
            border-bottom: 1px solid var(--gray-light);
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: linear-gradient(135deg, var(--blue), var(--crimson));
            color: white;
            position: sticky;
            top: 0;
            z-index: 10;
        }
        .modal-close {
            background: none;
            border: none;
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
        }
        .modal-body { padding: 24px; }
        
        .detail-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 20px;
        }
        .detail-item {
            background: var(--off-white);
            padding: 12px;
            border-radius: var(--radius-md);
        }
        .detail-item label {
            display: block;
            font-size: 0.8rem;
            color: var(--gray);
            margin-bottom: 4px;
        }
        .detail-item span {
            font-weight: 600;
            color: var(--gray-dark);
        }
        
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: var(--gray);
        }
        .empty-state i {
            font-size: 4rem;
            margin-bottom: 15px;
            opacity: 0.3;
        }
        
        .loading {
            text-align: center;
            padding: 40px;
        }
        .loading i {
            font-size: 2rem;
            color: var(--blue);
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            100% { transform: rotate(360deg); }
        }

        .chart-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }
        .chart-card {
            background: var(--white);
            border-radius: var(--radius-lg);
            padding: 20px;
            box-shadow: var(--shadow-sm);
        }
        .chart-title {
            font-weight: 700;
            color: var(--blue-dark);
            margin-bottom: 15px;
            text-align: center;
        }
        .bar-chart {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        .bar-item {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .bar-label {
            min-width: 100px;
            font-size: 0.85rem;
            color: var(--gray-dark);
        }
        .bar-wrapper {
            flex: 1;
            height: 24px;
            background: var(--gray-light);
            border-radius: 12px;
            overflow: hidden;
        }
        .bar-fill {
            height: 100%;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: flex-end;
            padding-right: 8px;
            color: white;
            font-size: 0.75rem;
            font-weight: 600;
            min-width: 30px;
        }

        @media (max-width: 768px) {
            .header { padding: 12px 15px; }
            .header-title h1 { font-size: 1.1rem; }
            .container { padding: 15px; }
            .stats-row { grid-template-columns: repeat(2, 1fr); }
            .stat-number { font-size: 1.5rem; }
        }
    </style>
</head>
<body>
    <div id="loginOverlay" class="login-overlay">
        <div class="login-card">
            <div class="login-logo">
                <i class="fas fa-comments"></i>
            </div>
            <h2 class="login-title">نظام تحليل الشكاوى</h2>
            <p class="login-subtitle">مجمع مكة الطبي بالزاهر</p>
            
            <select id="staffSelect" class="login-select">
                <option value="">جاري التحميل...</option>
            </select>
            
            <input type="password" id="passcodeInput" class="login-select" placeholder="الرمز السري" style="text-align:center; letter-spacing:8px; font-size:1.2rem;">
            
            <button id="loginBtn" class="login-btn" onclick="login()" disabled>
                <i class="fas fa-sign-in-alt"></i> دخول
            </button>
            <p id="loginError" class="login-error"></p>
        </div>
    </div>

    <div id="mainApp" style="display:none;">
        <header class="header">
            <div class="header-title">
                <i class="fas fa-comments" style="font-size:1.5rem;"></i>
                <h1>تحليل ومتابعة الشكاوى</h1>
            </div>
            <div class="header-user">
                <i class="fas fa-user"></i>
                <span id="userName"></span>
            </div>
            <div class="header-actions">
                <button class="btn btn-light" onclick="refreshData()">
                    <i class="fas fa-sync-alt"></i> تحديث
                </button>
                <a href="report.html" class="btn btn-light">
                    <i class="fas fa-plus"></i> رفع شكوى
                </a>
            </div>
        </header>

        <div class="container">
            <div class="tabs">
                <button class="tab-btn active" onclick="showTab('dashboard')">
                    <i class="fas fa-chart-pie"></i> لوحة المتابعة
                </button>
                <button class="tab-btn" onclick="showTab('complaints')">
                    <i class="fas fa-list"></i> الشكاوى <span class="count" id="complaintsCount">0</span>
                </button>
                <button class="tab-btn" onclick="showTab('new')">
                    <i class="fas fa-bell"></i> جديدة <span class="count" id="newCount">0</span>
                </button>
                <button class="tab-btn" onclick="showTab('inprogress')">
                    <i class="fas fa-spinner"></i> قيد المعالجة <span class="count" id="inprogressCount">0</span>
                </button>
            </div>

            <div id="dashboard" class="tab-content active">
                <div class="stats-row">
                    <div class="stat-card">
                        <div class="stat-number" id="totalComplaints">0</div>
                        <div class="stat-label">إجمالي الشكاوى</div>
                    </div>
                    <div class="stat-card warning">
                        <div class="stat-number" id="newComplaints">0</div>
                        <div class="stat-label">شكاوى جديدة</div>
                    </div>
                    <div class="stat-card orange">
                        <div class="stat-number" id="inProgressComplaints">0</div>
                        <div class="stat-label">قيد المعالجة</div>
                    </div>
                    <div class="stat-card success">
                        <div class="stat-number" id="closedComplaints">0</div>
                        <div class="stat-label">تم الحل</div>
                    </div>
                    <div class="stat-card danger">
                        <div class="stat-number" id="avgDays">0</div>
                        <div class="stat-label">متوسط أيام الحل</div>
                    </div>
                </div>

                <div class="chart-container">
                    <div class="chart-card">
                        <div class="chart-title"><i class="fas fa-chart-bar"></i> توزيع الشكاوى</div>
                        <div id="typeChart" class="bar-chart"></div>
                    </div>
                    <div class="chart-card">
                        <div class="chart-title"><i class="fas fa-tasks"></i> حسب الحالة</div>
                        <div id="statusChart" class="bar-chart"></div>
                    </div>
                </div>
            </div>

            <div id="complaints" class="tab-content">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title"><i class="fas fa-list"></i> جميع الشكاوى</h3>
                    </div>
                    <div class="card-body">
                        <div class="table-container">
                            <table>
                                <thead>
                                    <tr>
                                        <th>رقم الشكوى</th>
                                        <th>التاريخ</th>
                                        <th>النوع</th>
                                        <th>الشاكي</th>
                                        <th>الحالة</th>
                                        <th>إجراء</th>
                                    </tr>
                                </thead>
                                <tbody id="complaintsTable"></tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

            <div id="new" class="tab-content">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title"><i class="fas fa-bell"></i> شكاوى جديدة</h3>
                    </div>
                    <div class="card-body">
                        <div class="table-container">
                            <table>
                                <thead>
                                    <tr>
                                        <th>رقم الشكوى</th>
                                        <th>التاريخ</th>
                                        <th>النوع</th>
                                        <th>الشاكي</th>
                                        <th>إجراء</th>
                                    </tr>
                                </thead>
                                <tbody id="newTable"></tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

            <div id="inprogress" class="tab-content">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title"><i class="fas fa-spinner"></i> قيد المعالجة</h3>
                    </div>
                    <div class="card-body">
                        <div class="table-container">
                            <table>
                                <thead>
                                    <tr>
                                        <th>رقم الشكوى</th>
                                        <th>التاريخ</th>
                                        <th>النوع</th>
                                        <th>المكلف</th>
                                        <th>إجراء</th>
                                    </tr>
                                </thead>
                                <tbody id="inprogressTable"></tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div id="detailModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3><i class="fas fa-file-alt"></i> تفاصيل الشكوى</h3>
                <button class="modal-close" onclick="closeModal()">&times;</button>
            </div>
            <div class="modal-body" id="modalBody"></div>
        </div>
    </div>

    <script>
        const SCRIPT_URL = "https://script.google.com/macros/s/AKfycbyE0nbyfYrDZeOHr6TuEQP7ivpOLF1pLmBFoF7fkDmNjQp5g6dW4i0xkcSBJJRaoOCVAw/exec";
        
        const STATUS = {
            'new': { name: 'جديدة', badge: 'warning' },
            'in_progress': { name: 'قيد المعالجة', badge: 'info' },
            'resolved': { name: 'تم الحل', badge: 'success' },
            'closed': { name: 'مغلقة', badge: 'secondary' }
        };
        
        let currentUser = null;
        let allComplaints = [];

        async function apiCall(action, payload = {}) {
            try {
                const response = await fetch(SCRIPT_URL, {
                    method: 'POST',
                    headers: { 'Content-Type': 'text/plain' },
                    body: JSON.stringify({ action, payload }),
                    redirect: 'follow'
                });
                const text = await response.text();
                return JSON.parse(text);
            } catch (err) {
                console.error('API Error:', err);
                return { ok: false, error: err.message };
            }
        }

        async function loadStaff() {
            try {
                const result = await apiCall('getComplaintStaff');
                if (result.ok && result.staff && result.staff.length > 0) {
                    const select = document.getElementById('staffSelect');
                    select.innerHTML = '<option value="">اختر اسمك للدخول</option>';
                    result.staff.forEach(s => {
                        select.innerHTML += `<option value="${s.name}">${s.name}</option>`;
                    });
                    document.getElementById('loginBtn').disabled = false;
                } else {
                    document.getElementById('staffSelect').innerHTML = '<option value="">لا يوجد موظفين</option>';
                }
            } catch (err) {
                console.error(err);
                document.getElementById('staffSelect').innerHTML = '<option value="">خطأ في التحميل</option>';
            }
        }

        async function login() {
            const staffName = document.getElementById('staffSelect').value;
            const passcode = document.getElementById('passcodeInput').value;
            const errorEl = document.getElementById('loginError');
            
            if (!staffName) {
                errorEl.textContent = 'اختر اسمك';
                errorEl.style.display = 'block';
                return;
            }
            if (!passcode) {
                errorEl.textContent = 'أدخل الرمز السري';
                errorEl.style.display = 'block';
                return;
            }
            
            try {
                const result = await apiCall('verifyComplaintPasscode', { staffName, passcode });
                if (result.ok && result.verified) {
                    currentUser = staffName;
                    document.getElementById('loginOverlay').classList.add('hidden');
                    document.getElementById('mainApp').style.display = 'block';
                    document.getElementById('userName').textContent = staffName;
                    loadDashboard();
                } else {
                    errorEl.textContent = result.error || 'الاسم أو الرمز غير صحيح';
                    errorEl.style.display = 'block';
                }
            } catch (err) {
                errorEl.textContent = 'خطأ في الاتصال بالسيرفر';
                errorEl.style.display = 'block';
            }
        }

        function showTab(tabId) {
            document.querySelectorAll('.tab-btn').forEach(btn => btn.classList.remove('active'));
            document.querySelectorAll('.tab-content').forEach(content => content.classList.remove('active'));
            
            event.target.closest('.tab-btn').classList.add('active');
            document.getElementById(tabId).classList.add('active');
        }

        async function loadDashboard() {
            try {
                const statsResult = await apiCall('getComplaintStats', {});
                if (statsResult.ok) {
                    document.getElementById('totalComplaints').textContent = statsResult.total || 0;
                    document.getElementById('newComplaints').textContent = statsResult.new || 0;
                    document.getElementById('inProgressComplaints').textContent = statsResult.in_progress || 0;
                    document.getElementById('closedComplaints').textContent = statsResult.closed || 0;
                    document.getElementById('avgDays').textContent = statsResult.avgResolutionTime || 0;
                    
                    document.getElementById('complaintsCount').textContent = statsResult.total || 0;
                    document.getElementById('newCount').textContent = statsResult.new || 0;
                    document.getElementById('inprogressCount').textContent = statsResult.in_progress || 0;
                    
                    renderCharts(statsResult);
                }
                
                loadAllComplaints();
            } catch (err) {
                console.error(err);
            }
        }

        function renderCharts(stats) {
            const typeChart = document.getElementById('typeChart');
            const statusChart = document.getElementById('statusChart');
            
            const colors = ['#0056A3', '#9E1B32', '#28a745', '#ffc107', '#6f42c1', '#fd7e14'];
            
            if (stats.byType) {
                let typeHtml = '';
                const types = Object.entries(stats.byType);
                const maxType = Math.max(...types.map(t => t[1]));
                
                types.forEach(([type, count], i) => {
                    const pct = maxType > 0 ? (count / maxType * 100) : 0;
                    typeHtml += `
                        <div class="bar-item">
                            <span class="bar-label">${type}</span>
                            <div class="bar-wrapper">
                                <div class="bar-fill" style="width:${pct}%; background:${colors[i % colors.length]}">${count}</div>
                            </div>
                        </div>
                    `;
                });
                typeChart.innerHTML = typeHtml || '<p style="text-align:center;color:#999;">لا توجد بيانات</p>';
            }
            
            const statusData = [
                { name: 'جديدة', count: stats.new || 0, color: '#ffc107' },
                { name: 'قيد المعالجة', count: stats.in_progress || 0, color: '#0056A3' },
                { name: 'تم الحل', count: stats.closed || 0, color: '#28a745' }
            ];
            const maxStatus = Math.max(...statusData.map(s => s.count));
            
            let statusHtml = '';
            statusData.forEach(s => {
                const pct = maxStatus > 0 ? (s.count / maxStatus * 100) : 0;
                statusHtml += `
                    <div class="bar-item">
                        <span class="bar-label">${s.name}</span>
                        <div class="bar-wrapper">
                            <div class="bar-fill" style="width:${pct}%; background:${s.color}">${s.count}</div>
                        </div>
                    </div>
                `;
            });
            statusChart.innerHTML = statusHtml;
        }

        async function loadAllComplaints() {
            try {
                const result = await apiCall('getComplaints', {});
                if (result.ok && result.complaints) {
                    allComplaints = result.complaints;
                    renderTable('complaintsTable', allComplaints);
                    renderTable('newTable', allComplaints.filter(c => c.status === 'new'));
                    renderTable('inprogressTable', allComplaints.filter(c => c.status === 'in_progress'));
                }
            } catch (err) {
                console.error(err);
            }
        }

        function renderTable(tableId, complaints) {
            const tbody = document.getElementById(tableId);
            if (!complaints || complaints.length === 0) {
                tbody.innerHTML = '<tr><td colspan="6" style="text-align:center;padding:40px;color:#999;">لا توجد شكاوى</td></tr>';
                return;
            }
            
            let html = '';
            complaints.forEach(c => {
                const statusInfo = STATUS[c.status] || { name: c.status, badge: 'secondary' };
                html += `
                    <tr>
                        <td>${c.id}</td>
                        <td>${c.date}</td>
                        <td>${c.type}</td>
                        <td>${c.complainant || 'مجهول'}</td>
                        <td><span class="badge badge-${statusInfo.badge}">${statusInfo.name}</span></td>
                        <td><button class="btn btn-primary btn-sm" onclick="viewDetails('${c.id}')"><i class="fas fa-eye"></i></button></td>
                    </tr>
                `;
            });
            tbody.innerHTML = html;
        }

        async function viewDetails(complaintId) {
            try {
                const result = await apiCall('getComplaintDetails', { complaintId });
                if (result.ok && result.complaint) {
                    const c = result.complaint;
                    const statusInfo = STATUS[c.status] || { name: c.status, badge: 'secondary' };
                    
                    let html = `
                        <div class="detail-grid">
                            <div class="detail-item">
                                <label>رقم الشكوى</label>
                                <span>${c.id}</span>
                            </div>
                            <div class="detail-item">
                                <label>تاريخ التقديم</label>
                                <span>${c.submitDate} ${c.submitTime}</span>
                            </div>
                            <div class="detail-item">
                                <label>نوع الشكوى</label>
                                <span>${c.type}</span>
                            </div>
                            <div class="detail-item">
                                <label>الحالة</label>
                                <span class="badge badge-${statusInfo.badge}">${statusInfo.name}</span>
                            </div>
                            <div class="detail-item">
                                <label>اسم الشاكي</label>
                                <span>${c.complainantName || 'غير محدد'}</span>
                            </div>
                            <div class="detail-item">
                                <label>رقم الهاتف</label>
                                <span>${c.complainantPhone || 'غير محدد'}</span>
                            </div>
                            <div class="detail-item">
                                <label>الموقع</label>
                                <span>${c.locations || 'غير محدد'}</span>
                            </div>
                            <div class="detail-item">
                                <label>المكلف</label>
                                <span>${c.assignedTo || 'غير مكلف'}</span>
                            </div>
                        </div>
                        <div class="detail-item" style="margin-bottom:15px;">
                            <label>وصف الشكوى</label>
                            <p style="margin-top:8px;line-height:1.8;">${c.description || 'لا يوجد وصف'}</p>
                        </div>
                    `;
                    
                    document.getElementById('modalBody').innerHTML = html;
                    document.getElementById('detailModal').classList.add('active');
                }
            } catch (err) {
                console.error(err);
            }
        }

        function closeModal() {
            document.getElementById('detailModal').classList.remove('active');
        }

        function refreshData() {
            loadDashboard();
        }

        document.addEventListener('DOMContentLoaded', loadStaff);
        
        document.getElementById('passcodeInput').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') login();
        });
    </script>
</body>
</html>
