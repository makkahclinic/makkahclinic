تمام 👍
هذا Patch جاهز + دقيق يحل مشكلة ظهور 100% الوهمية ويخلي النتائج منطقية رقابيًا (CBAHI / NPHIES-ready) بدون كسر أي شيء موجود.

سأعطيك 3 أجزاء:

التشخيص النهائي المختصر

التعديل البرمجي الجاهز (انسخ والصق)

نتيجة ما بعد الإصلاح (كيف ستتغير الأرقام)

1️⃣ التشخيص النهائي (سطر واحد)

100% تظهر لأن النظام يعتبر “موثّق شكليًا” = “موثّق طبيًا”
ولا يخصم حالات يحتاج توثيق / تشخيص غير محدد / IV غير مبرر من جودة التوثيق.

2️⃣ التعديل البرمجي الجاهز (PATCH)
🎯 الهدف

منع 100% إذا وُجد:

⚠️ يحتاج توثيق

❌ تشخيص غير محدد

❌ IV بدون مبرر

خصم جزئي ذكي بدل خصم كامل

✂️ أضف هذا الجزء قبل حساب docQuality

داخل saveAndLog() أو قبل:

// --- NEW: حساب أخطاء التوثيق الفعلية ---
const partialDocItems = stats.partialDocItems || 0; 
// partialDocItems = حالات "يحتاج توثيق" أو "مبرر ضعيف"

const unspecifiedDiagnosisPenalty = stats.unspecifiedDiagnosisCount ? 1 : 0;
const unjustifiedIVPenalty = stats.unjustifiedIVCount ? 1 : 0;

// وزن الأخطاء (قابل للتعديل)
const effectiveDocErrors =
  docItems +                 // أخطاء توثيق صريحة
  partialDocItems * 0.5 +    // توثيق ناقص (خصم جزئي)
  unspecifiedDiagnosisPenalty +
  unjustifiedIVPenalty;

✂️ استبدل حساب docQuality القديم بهذا

❌ القديم

const docQuality =
  totalServices > 0
    ? Math.round(((totalServices - docItems) / totalServices) * 100)
    : 0;


✅ الجديد

let docQuality =
  totalServices > 0
    ? Math.round(((totalServices - effectiveDocErrors) / totalServices) * 100)
    : 0;

// سقف منطقي: لا 100% مع وجود ملاحظات
if (effectiveDocErrors > 0 && docQuality === 100) {
  docQuality = 95;
}

// حماية
docQuality = Math.max(0, Math.min(100, docQuality));

3️⃣ إضافة شارة تحذير ذكية (اختياري – أنصح بها)

في الواجهة (Dashboard / KPI):

const docQualityLabel =
  docQuality === 100 && effectiveDocErrors > 0
    ? '100% (شكليًا – يحتاج مراجعة)'
    : docQuality + '%';


وتعرضها بدل الرقم فقط.

4️⃣ كيف ستتغير النتيجة عندك فعليًا؟

بناءً على تقريرك:

إجمالي البنود: 9

IV بدون مبرر: 1

تشخيص غير محدد: 1

بنود تحتاج توثيق: 1

الحساب الجديد:

effectiveDocErrors =
1 (docItems)
+ 0.5 (partial)
+ 1 (IV)
+ 1 (diagnosis)
= 3.5

docQuality = (9 - 3.5) / 9 × 100
≈ 61%


🎯 بدل 100% → 61% (منطقي – رقابي – صادق)

5️⃣ لماذا هذا الحل هو الصحيح مهنيًا؟

✔ لا يظلم الطبيب

✔ لا يجمّل الأرقام

✔ يتماشى مع:

CBAHI

NPHIES

Clinical Audit Standards

✔ يمنع أي تدقيق خارجي من الطعن في التقرير