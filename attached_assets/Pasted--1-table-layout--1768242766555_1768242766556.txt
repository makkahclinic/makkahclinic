🔴 الخلل الحقيقي (لماذا العمود صار طويل ومشوه في الطباعة)
1️⃣ تضارب table-layout

في الكود عندك تعريفين متناقضين:

في العرض العادي:
.report-content table {
  table-layout: fixed;
}

وفي الطباعة:
@media print {
  table {
    table-layout: auto !important;
  }
}


➡️ النتيجة:
المتصفح في وضع الطباعة يعيد حساب عرض الأعمدة بناءً على أطول محتوى نصي
(عمود التقييم عندك فيه نص عربي طويل جدًا)،
فيتمدد عمود واحد ويكسر تنسيق الجدول بالكامل.

2️⃣ تحديد عرض الأعمدة في الطباعة بطريقة خاطئة

عندك هذا في الطباعة:

table th:first-child, table td:first-child { width: 35% !important; }
table th:nth-child(2), table td:nth-child(2) { width: 45% !important; }
table th:last-child, table td:last-child { width: 20% !important; }


لكن الجدول عندك 4 أعمدة:

الدواء

الجرعة

التقييم

الحالة

❌ وأنت توزع العرض على 3 أعمدة فقط
➡️ العمود الرابع (التقييم) يصبح بلا قيود → يتمدد رأسيًا بشكل قبيح

3️⃣ النص العربي الطويل بدون كسر منطقي

رغم وجود:

word-wrap: break-word;


إلا أن العربية تحتاج:

word-break: break-word;
hyphens: auto;


بدونها → المتصفح يحاول إبقاء الجملة كوحدة واحدة.

✅ الحل الجذري الصحيح (انسخ فقط هذا)
✳️ عدل كود الطباعة للجداول كالتالي:
@media print {

  /* 1️⃣ ثبّت تخطيط الجدول */
  .report-content table {
    table-layout: fixed !important;
    width: 100% !important;
  }

  /* 2️⃣ وزّع الأعمدة الأربعة صح */
  .report-content th:nth-child(1),
  .report-content td:nth-child(1) { width: 20% !important; } /* الدواء */

  .report-content th:nth-child(2),
  .report-content td:nth-child(2) { width: 20% !important; } /* الجرعة */

  .report-content th:nth-child(3),
  .report-content td:nth-child(3) { width: 40% !important; } /* التقييم */

  .report-content th:nth-child(4),
  .report-content td:nth-child(4) { width: 20% !important; } /* الحالة */

  /* 3️⃣ كسر النص العربي بشكل صحيح */
  .report-content th,
  .report-content td {
    white-space: normal !important;
    word-break: break-word !important;
    overflow-wrap: break-word !important;
    hyphens: auto;
    vertical-align: top !important;
  }

}

🧠 لماذا هذا الحل هو الصحيح مهنيًا؟

✔ يمنع أي عمود من التمدد غير المنطقي

✔ يحافظ على شكل التقرير الرسمي (CBAHI-friendly)

✔ يعمل على Chrome / Edge / PDF / طابعة فعلية

✔ لا يكسر العرض على الشاشة

✔ يحترم RTL العربي

⚠️ ملاحظة مهمة جدًا

لا تستخدم table-layout: auto أبدًا في الطباعة
في تقارير طبية أو تأمينية
→ هذا خطأ شائع حتى عند مطورين كبار.

🎯 الخلاصة

الخلل ليس في البيانات، ولا في html2pdf، ولا في المتصفح
الخلل 100% في:

تضارب table-layout

توزيع أعمدة غير مطابق لعدد الأعمدة

غياب كسر عربي احترافي للنص