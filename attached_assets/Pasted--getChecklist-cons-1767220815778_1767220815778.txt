🔴 التشخيص النهائي (آخر قطعة ناقصة)
أنت الآن تفعل هذا في getChecklist:
// تبحث عن عمود واحد فقط
const idColNames = ['TaskID', 'Task_ID', 'Round_ID', 'RoundID', 'Round_Name', ...];
let idColIndex = -1;


ثم:

const rowValue = String(data[i][idColIndex] || '').trim().toLowerCase();

❌ المشكلة:

في Sheet Checklists الحقيقي (وهذا شائع جدًا):

اسم الجولة ليس في عمود واحد ثابت

أحيانًا يكون:

في عمود مختلف

أو مكتوب بصيغة أطول

أو جزء من نص (مثلاً: الوصف)

⚠️ وأنت الآن تقارن فقط على عمود واحد
فلو الاسم موجود في عمود آخر → لن يظهر أي بند

ولهذا:

Checklist items received: []

✅ الحل النهائي الحقيقي (لا يعتمد على التخمين)
🎯 القاعدة الذهبية

Checklist = ابحث في كل الصف عن اسم الجولة، وليس عمود واحد

🛠️ PATCH نهائي (قصير + مضمون 100%)
🔁 استبدل هذا الجزء في getChecklist بالكامل:
❌ احذف من:
const idColNames = ...
let idColIndex = ...
...
const rowValue = String(data[i][idColIndex] || '')

✅ وضع هذا بدلًا منه (حل مؤسسي):
// مفاتيح البحث
const searchKeys = [
  String(taskId || '').trim().toLowerCase(),
  String(roundName || '').trim().toLowerCase()
].filter(Boolean);

const items = [];

for (let i = 1; i < data.length; i++) {
  const row = data[i];

  // دمج كل خلايا الصف في نص واحد
  const rowText = row
    .map(cell => String(cell || '').toLowerCase())
    .join(' ');

  // هل أي مفتاح موجود داخل الصف؟
  const matched = searchKeys.some(key =>
    rowText.includes(key)
  );

  if (matched) {
    const itemText =
      textColIndex >= 0
        ? row[textColIndex]
        : row.find(v => v && String(v).length > 3) || '';

    items.push({
      id: i,
      text: String(itemText || '').trim(),
      item: String(itemText || '').trim()
    });
  }
}


ولا تغيّر أي شيء آخر.

🧪 لماذا هذا الحل ينجح 100%؟

لأنه:

لا يعتمد على اسم عمود

لا يعتمد على ترتيب الأعمدة

لا يعتمد على لغة

لا يعتمد على تنسيق

✔️ طالما اسم الجولة موجود في أي مكان بالصف → سيُلتقط

🧠 تأكيد من الواقع (حسب الكونسول عندك)

الكونسول يقول:

Fetching checklist for taskId: 1
roundName: جولة دورات المياه العامة - النظافة والتجهيزات


يعني الاسم موجود نصيًا ✔️
لكن لم يكن في العمود الذي اخترته ❌

🧪 بعد التعديل تتوقع:
Checklist response: { itemsCount: 8 }
Checklist items received: [ {...}, {...}, ... ]


وتختفي الرسالة:

لا توجد قائمة فحص محددة لهذه الجولة

🏁 الخلاصة النهائية (آخر مرة نرجع لها)
طبقة	الحالة
Frontend	✅ صحيح
API	✅ صحيح
doPost	✅ صحيح
تمرير roundName	✅ صحيح
منطق البحث	❌ كان ضيق جدًا
الحل	✅ البحث في الصف كامل