تمام، راجعت الكود + الشيت + التقرير المرفق بدقة، والمشكلة واضحة ومحددة وليست غامضة.
سبب ظهور 100% رغم أنك لا تُدخل رقم 100 يدويًا هو خلل منطقي (Logic Bug) ناتج عن اختلاط أعمدة + حساب تلقائي افتراضي.

سأشرحها نقطة بنقطة وبالدليل 👇

الخلاصة السريعة (TL;DR)

رقم 100% لا يأتي من المستخدم ولا من Excel مباشرة
بل يأتي من معادلة حسابية تلقائية في الواجهة (Dashboard / KPI) تعتمد على:

(عدد البنود الموثقة ÷ إجمالي البنود) × 100

ومع حالة معينة في بياناتك تصبح النتيجة = 100% حتى لو القيم الأصلية ليست 100.

أين يظهر 100% بالضبط؟

في التقرير المرفق يظهر:

توثيق العلامات الحيوية = 100%

أكواد ICD موجودة = 100%

(راجع صفحة الملخص النهائي – الصفحة 9 من الملف) 

١١١١١١

وهذا صحيح حسابيًا لكنه مضلل منطقيًا.

السبب الحقيقي (Root Cause)
1️⃣ قاعدة الحساب الحالية في النظام

من صفحة المنهجية – الصفحة 10:

إجمالي بنود الخدمة = عدد الصفوف الفعلية في Excel (9 بنود)
جودة التوثيق = (الإجراءات − أخطاء التوثيق) ÷ الإجراءات × 100 

١١١١١١

يعني:

لو ولا بند واحد مصنَّف كـ خطأ توثيق
→ النتيجة تلقائيًا = 100%

2️⃣ المشكلة المنطقية

في حالتك:

✔ العلامات الحيوية موجودة شكليًا في كل الحالات

❌ لكن غير كافية سريريًا (مثلاً: حرارة طبيعية بدون مبرر IV)

النظام حاليًا:

لا يفرّق بين:

موثّق شكليًا

موثّق طبيًا ومبررًا

➡️ فيُسجلها كلها = موثقة
➡️ النتيجة = 100%

الدليل من الكود (مهم جدًا)

في saveAndLog():

const docQuality =
  totalServices > 0
    ? Math.round(((totalServices - docItems) / totalServices) * 100)
    : 0;


إذا:

docItems = 0

totalServices = 9

⬅️ الناتج = (9 - 0) / 9 × 100 = 100

🔴 حتى لو كان التوثيق ضعيف سريريًا

خطأ إضافي خطير (Secondary Bug)
3️⃣ عدم احتساب “توثيق ناقص” كخطأ

في التقرير:

توجد حالات "يحتاج توثيق إضافي"

لكنها لا تُخصم من docQuality

مثال من الصفحة 5:

⚠ يحتاج توثيق إضافي – ESOPOLE 40 MG 

١١١١١١

لكن:

docItems = عدد "بنود تحتاج توثيق فقط"


ولا يتم خصم:

توثيق غير كاف

مبرر ضعيف

تشخيص غير محدد

لماذا هذا خطير؟

لأنه يعطي:

🔵 لوحة مؤشرات “جميلة”

🔴 لكنها غير دقيقة رقابيًا

وقد تُستخدم في:

تقييم طبيب

تقرير جودة رسمي

عرض إداري أو اعتماد

الحل الصحيح (الموصى به)
✅ الحل 1: فصل التوثيق إلى مستويين

بدل:

موثق / غير موثق

اجعل:

موثق شكليًا

موثق طبيًا

توثيق ناقص (يُخصم جزئيًا)

مثال:

effectiveDocErrors = docItems + partialDocItems * 0.5;

✅ الحل 2: منع 100% إذا وُجدت ملاحظات توثيق

شرط بسيط:

if (partialDocItems > 0) {
  docQuality = Math.min(docQuality, 95);
}

✅ الحل 3 (أفضل): إظهار شارة تحذير

حتى لو بقيت 100%:

⚠ 100% شكليًا – يحتاج مراجعة سريرية

هل Excel نفسه سبب المشكلة؟

❌ لا
Excel سليم 100%
والقيم فيه صحيحة
المشكلة في طبقة الحساب (Logic Layer) وليس في البيانات.

الخلاصة النهائية
العنصر	الحالة
هل المستخدم أدخل 100؟	❌ لا
هل Excel فيه 100؟	❌ لا
هل الحساب البرمجي يولده؟	✅ نعم
هل النتيجة مضللة؟	⚠️ نعم
هل يمكن إصلاحها؟	✅ بسهولة