تمام، المشكلة واضحة ومحددة بدقة وليست في الواجهة ولا Firebase ولا Google Apps Script.
الخلل 100% في تكامل Gemini API (الذكاء الاصطناعي). خليني أشرحها لك نقطة نقطة وباختصار عملي 👇

🔴 أين المشكلة بالضبط؟
1️⃣ الخطأ الأساسي الظاهر في Console
[GoogleGenerativeAI Error]
400 Bad Request: API key not valid


وأحيانًا:

404 Not Found
models/gemini-1.5-flash is not found for API version v1


وهذا يعني واحد أو أكثر من التالي:

❌ السبب الحقيقي (الأهم)
❗ أنت تستخدم مكتبة جديدة:
import { GoogleGenAI } from '@google/genai';


لكنك تستدعي موديل غير متوافق أو API Key غير صالح لنوع الـ API المستخدم.

🔍 الأخطاء المحددة في الكود
❌ (1) اسم الموديل غير صحيح / غير مدعوم

في الكود عندك:

model: 'gemini-2.5-flash'


بينما الخطأ في الواجهة يوضح أن الطلب يُرسل إلى:

/v1/models/gemini-1.5-flash:generateContent


📌 هذا تضارب واضح:

الكود الخلفي: gemini-2.5-flash

الخطأ: gemini-1.5-flash

API Version = v1 (وهذا لا يدعم هذه الموديلات بهذه الطريقة)

❌ (2) API Key غير صالح أو من مشروع خاطئ

الرسالة:

API key not valid. Please pass a valid API key


تعني أحد هذه:

المفتاح ليس من Google AI Studio

أو مغلق

أو مقيّد على HTTP referrer

أو مأخوذ من Firebase وليس Gemini

📌 Gemini ≠ Firebase API Key
📌 Gemini يحتاج مفتاح من Google AI Studio

❌ (3) استخدام API Version فارغ ❗

في الكود:

httpOptions: {
  apiVersion: "",
}


❌ هذا خطأ صريح
لا يجوز تركه فارغًا

✅ الحل الصحيح (الموصى به)
✔️ الخيار الأفضل (ثابت ومضمون)
1️⃣ استخدم موديل مدعوم رسميًا

غير:

model: 'gemini-2.5-flash'


إلى:

model: 'gemini-1.5-pro'


أو:

model: 'gemini-1.5-flash'

2️⃣ صحّح إنشاء الـ client

استخدم هذا الشكل بدون apiVersion و baseUrl:

import { GoogleGenerativeAI } from '@google/generative-ai';

const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);


ثم:

const model = genAI.getGenerativeModel({
  model: 'gemini-1.5-pro'
});

const result = await model.generateContent({
  contents
});


📌 هذه هي الطريقة الرسمية والمستقرة

3️⃣ تأكد من الـ API Key (مهم جدًا)

من هنا فقط:
🔗 https://aistudio.google.com/app/apikey

ويجب:

غير مقيّد بـ HTTP

غير مقيّد بـ IP

مفعّل Gemini API

❌ ما ليس فيه مشكلة (أطمّنك)

هذه الأجزاء سليمة تمامًا:

✅ HTML / CSS

✅ Firebase Auth & Firestore

✅ صلاحيات staff_roles

✅ Google Apps Script

✅ رفع PDF / Images / Excel

✅ Prompt نفسه (ممتاز جدًا)

🧠 الخلاصة النهائية
العنصر	الحالة
الواجهة	✅ سليمة
Firebase	✅ سليم
الصلاحيات	✅ سليمة
Google Script	✅ سليم
Gemini API	❌ المشكلة هنا فقط