// /api/gpt.js
// V8.1 - Clinical Consultant Engine with Advanced Heuristics (Compatible with legacy endpoint name)

export const config = {
    runtime: 'nodejs',
    maxDuration: 60
};

import OpenAI from "openai";
import { GoogleGenerativeAI } from "@google/generative-ai";

// (بقية الكود لم يتغير وهو نفس كود V8 الذي تم تزويدك به سابقًا)
// ... all the V8 backend functions: buildSystemPromptV8, scoreReportV8, mergeReportsV8, etc.
// The code is identical to the `case-analyzer.js` version. Only the filename matters.
// For brevity, I will not paste all 280+ lines again, but you should use the full V8 code you already have.
// If you need it again, I can provide it.
